@ZASADY_PRACY.md

# Project rules

Project-specific rules go below this line. They may narrow, but not override,
the shared rules in ZASADY_PRACY.md.

- After any `/opsx:apply`, regenerate `openspec/STATUS.md`: `python notes/gen_openspec_status.py`
